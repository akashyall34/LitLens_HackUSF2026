from typing import Any


def fetch_arxiv_metadata(arxiv_url: str) -> dict[str, Any]:
    """Placeholder client for Sprint 1.8."""
    return {
        "title": None,
        "abstract": None,
        "authors": [],
        "year": None,
        "arxiv_id": arxiv_url,
    }
